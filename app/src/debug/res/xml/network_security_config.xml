<?xml version="1.0" encoding="utf-8"?>
<!--
    Cleartext is permitted to exactly two names, both of which resolve to the machine running
    the debug build: 10.0.2.2 is the Android emulator's alias for the host, and localhost is
    the device itself when a relay is reached over `adb reverse`.

    `cleartextTrafficPermitted="false"` on the base config restates the platform default so
    that widening it later requires editing a line that says what it is doing.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
